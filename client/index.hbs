<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Netflix User Verification</title>
        <style>
            body{
                background-color: #141414;
                font-family: 'Open Sans', 'Helvetica Neue', sans-serif
            }
            .content{
                width: 100%;
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
            }
            .content .account{
                text-align: center;
                color: #fff;
                margin-bottom: 12px;

            }
            .content .desc{
                color:grey;
                margin-bottom: 8px;
            }
            .content .links-list{
                margin: 0;
                padding: 0;
                width: 90%;
                max-width: 500px;
                border-radius: 4px;
                overflow: hidden;
                max-height: calc(100vh - 140px);
                overflow-y: auto;
            }
            .content .links-list::-webkit-scrollbar-track {
                padding: 2px 0;
                background-color: #404040;
                border-top-right-radius: 4px;
                border-bottom-right-radius: 4px;
            }

            .content .links-list::-webkit-scrollbar {
                width: 10px;
            }

            .content .links-list::-webkit-scrollbar-thumb {
            border-radius: 10px;
            box-shadow: inset 0 0 6px rgba(0,0,0,.3);
            background-color: #737272;
            border: 1px solid #000;
            }                     
            .content .links-list .item{
                position: relative;
                min-height: 40px;
                display: flex;
                justify-content: space-between;
                align-items: center;
                background-color: rgba(109,109,110,0.7);
                color: white;
                padding: 4px 12px;
            }
            .content .links-list .item::before{
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                width: 100%;
                height: 1px;
                background-color: #141414;
            }
            .content .links-list .item a{
                background-color: white;
                padding: 4px 8px;
                color: black;
                border-radius: 4px;
                text-decoration: none;
                min-width: 50px;
                text-align: center;
            }
        </style>
    </head>
    <body>
        <div class="content">
            <h1 class="account">{{account}}</h1>
            <span class="desc">Todas las solicitudes</span>
            <ul class="links-list">
                {{#each verificationUrls}}
                    <li class="item">
                        <span>Fecha: {{this.date.fulldate}} - Hora: {{this.date.fulltime}}</span>
                        <a href="{{this.url}}" target="_blank">Link</a>
                    </li>
                {{/each}}
            </ul>
        </div>
    </body>
</html>